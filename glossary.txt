-------------------------------------------------------------------
Access Control List (Список контролю доступу) 🔐
Access Control List (ACL) — це набір правил, які визначають, хто або що має доступ до певного ресурсу та які операції йому дозволені. 
Цей механізм використовується для забезпечення безпеки в комп'ютерних системах та мережах.
-------------------------------------------------------------------
CSRF означає Cross-Site Request Forgery (підробка міжсайтових запитів). 
Це тип атаки, при якій зловмисник змушує авторизованого користувача виконати небажану дію на вебсайті, який користувач вважає надійним.

Захист від CSRF
Найпоширенішим і ефективним способом захисту є використання CSRF-токенів.
Генерація токена: Сервер генерує унікальний, непередбачуваний токен для кожної сесії користувача.
Передача токена: Цей токен передається клієнту, зазвичай як приховане поле у формі або в заголовку HTTP.
Відправлення токена: Коли користувач надсилає запит (наприклад, форму), він також відправляє цей токен назад на сервер.
Перевірка: Сервер перевіряє, чи отриманий токен збігається з тим, що зберігається на сервері для цієї сесії. Якщо токени не збігаються, запит відхиляється.
-------------------------------------------------------------------
 Retrieval-Augmented Generation (RAG)
 
 
 
 -------------------------------------------------------------------
SoC vs SOLID
SoC (Separation of Concerns) — Поділ Обов'язків.
Що це: Головний архітектурний принцип. Розділяє систему на незалежні модулі/шари (UI, Бізнес-логіка, Дані).
Мета: Чистота та модульність коду.
SRP (Single Responsibility Principle) — Принцип Єдиної Відповідальності.
Що це: Перший принцип SOLID (S). Клас повинен мати тільки одну причину для зміни.
Зв'язок: SRP — це втілення SoC на рівні окремого класу.


UX (User Experience) — це досвід, який отримує користувач під час взаємодії з продуктом, сервісом або інтерфейсом.