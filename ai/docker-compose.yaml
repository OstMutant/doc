version: "3.9"

# =================================================================
# 🚀 ПОВНИЙ АЛГОРИТМ ДІЙ: ВІД ЗАПУСКУ ДО РОБОТИ В IDE
# =================================================================
# 
# 🛠 КРОК 1: ЗАПУСК У ТЕРМІНАЛІ
#    1. Відкрийте PowerShell або WSL у папці з цим файлом.
#    2. Введіть: docker compose up -d
#
# 📥 КРОК 2: МОНІТОРИНГ ТА ПЕРЕВІРКА
#    1. Введіть: docker logs -f ollama-init
#    2. 🧪 Перевірка моделей: docker exec -it ollama ollama list
#       (Має бути: qwen2.5:14b, qwen2.5-coder:7b, nomic-embed-text)
#
# 🌐 КРОК 3: ВХІД В OPEN WEBUI (Для документів .md)
#    1. Відкрийте: http://localhost:3000
#    2. Завантажуйте доки в "Workspace" -> "Documents".
#
# 💻 КРОК 4: ПІДКЛЮЧЕННЯ ДО INTELLIJ IDEA (Рідний AI Плагін)
#    1. Settings -> Tools -> AI Assistant.
#    2. Увімкніть "Enable Local Models" та оберіть провайдер "Ollama".
#    3. Вкажіть адресу: http://localhost:11434
#    4. Chat: qwen2.5:14b | Code Completion: qwen2.5-coder:7b
#    5. ПРИМІТКА: Контекст 16384 вже вшитий у модель автоматично!
# =================================================================

services:
  # --- Двигун моделей (GPU) ---
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    restart: always
    environment:
      - OLLAMA_NUM_PARALLEL=4
      - OLLAMA_MAX_LOADED_MODELS=2
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 5s
      retries: 5

  # --- Авто-інсталятор з вшитим контекстом 16k ---
  ollama-init:
    image: ollama/ollama:latest
    container_name: ollama-init
    environment:
      - OLLAMA_HOST=ollama:11434
    volumes:
      - ollama_models:/root/.ollama
    depends_on:
      ollama:
        condition: service_healthy
    command: >
      sh -c "sleep 5 && 
             ollama pull qwen2.5:14b && 
             ollama pull qwen2.5-coder:7b && 
             ollama pull nomic-embed-text && 
             echo 'FROM qwen2.5:14b\nPARAMETER num_ctx 16384' > /tmp/Modelfile && 
             ollama create qwen2.5:14b -f /tmp/Modelfile"
    restart: on-failure

  # --- Векторна база даних ---
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    restart: always
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage
    shm_size: "1gb"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6333/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5

  # --- Інтерфейс користувача (RAG менеджер) ---
  openwebui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: openwebui
    restart: always
    ports:
      - "3000:3000"
    environment:
      - WEBUI_PORT=3000
      - OLLAMA_BASE_URL=http://ollama:11434
      - VECTOR_DB_TYPE=qdrant
      - VECTOR_DB_URL=http://qdrant:6333
      - RAG_EMBEDDING_ENGINE=ollama
      - RAG_EMBEDDING_MODEL=nomic-embed-text
      - RAG_TOP_K=10
      - RAG_RELEVANCE_THRESHOLD=0.4
      - ENABLE_RAG_WEB_SEARCH=True
      - WEBUI_SECRET_KEY=ost_secret_key_2025_strong
      - WEBUI_AUTH=True
      - DEFAULT_MODEL=qwen2.5:14b
    volumes:
      - openwebui_data:/app/data
    depends_on:
      ollama:
        condition: service_healthy
      qdrant:
        condition: service_healthy
    user: root
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  ollama_models:
  openwebui_data:
  qdrant_data: